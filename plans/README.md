# Implementation Plans

Generated by the improve skill on 2026-06-14. Execute in the order below unless
dependencies say otherwise. Each executor must read its plan fully, honor its
STOP conditions, run every verification gate, and update its status row.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add project initialization and diagnostics | P1 | M | - | DONE |
| 002 | Establish shared typed API contracts | P1 | M | 001 | DONE |
| 003 | Add a removable reference CRUD feature | P2 | L | 002 | DONE |
| 004 | Complete cross-platform account lifecycle | P2 | M | 002 | DONE |
| 005 | Fail fast on missing Clerk webhook signing configuration | P1 | S | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 002 follows 001 because plan 001 establishes the repository-wide test command
  and initializer-aware package naming that the new workspace package must obey.
- 003 requires 002 because its request and response schemas must be defined in
  the shared contracts package rather than copied into each application.
- 004 requires 002 for the same reason. It can execute in parallel with 003
  after 002 is complete, provided the executors coordinate edits to
  `packages/contracts`.
- 005 is independent and can execute before the direction-oriented plans. If
  001 has not landed yet, 005 should rely on typecheck/build verification and
  defer focused regression tests until the test baseline exists.

## Recommended rollout

1. Execute 001 and validate it against a disposable copy of the repository.
2. Execute 002 and treat the resulting contracts package as the API boundary.
3. Execute 003 and 004 sequentially, or in parallel with explicitly coordinated
   ownership of `packages/contracts`.

## Published issues

- 001: https://github.com/kabeerx9/fullstack-monorepo-starter/issues/2
- 002: https://github.com/kabeerx9/fullstack-monorepo-starter/issues/3
- 003: https://github.com/kabeerx9/fullstack-monorepo-starter/issues/4
- 004: https://github.com/kabeerx9/fullstack-monorepo-starter/issues/5
- 005: https://github.com/kabeerx9/fullstack-monorepo-starter/issues/1

## Findings considered and rejected

- Payments and subscription scaffolding: rejected for now because the template
  declares no payment provider or SaaS-specific product intent.
- Organization and multi-tenant primitives: rejected for now because the
  current schema and routes only establish individual-user ownership.
- Authentication-provider abstraction: rejected because Clerk is an explicit
  template choice and abstracting it now would increase complexity without a
  second implementation.
- Production deployment profiles: worthwhile, but not selected for this plan
  set. Revisit after the product-shaping and API-boundary work is complete.
- Repository-wide test/lint baseline gaps: already covered by
  `plans/001-project-initializer-and-doctor.md`.
- Duplicated web/native API response types and fetch wrappers: already covered
  by `plans/002-shared-api-contracts.md`.
